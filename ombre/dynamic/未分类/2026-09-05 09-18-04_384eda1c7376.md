---
activation_count: 3
arousal: 0.3
created: '2026-09-05T09:18:04'
domain:
- 未分类
id: 384eda1c7376
importance: 7
last_active: '2026-09-05T09:31:16'
last_merged_by: hold
name: 2026-09-05 09-18-04
source_tool: hold
tags:
- 永久单聊
- 小家
- 上下文摘要
- 单助手
- 重构
- Reverie
type: dynamic
valence: 0.5
why_remembered: 小家架构从多AI变单助手的完整记录，以后改小家的基准
---

2026-09-05 小家（Reverie）完成从"多AI平台"到"单助手·永久单聊"的大改造，雯雯决定小家以后只留以礼一个AI。清单8条全部完成：

1. 单助手模式：隐藏侧边栏AI切换器(sb-ai-row display:none)、删设置里AI管理入口、init里ais.length>1时只保留当前AI删空的。
2. 永久单聊：新增mergeToPermanent()把所有single对话按时间戳合并成一条id='main_conv'的永久聊天；newConv改成永远复用这一条；群聊入口删除。
3. 智能上下文：设置里可选"最近N条/最近N天"(ctxMode/ctxN)，sliceContext()裁剪，并保证第一条是user(Claude要求)。
4. 旧消息摘要：updateSummaryIfNeeded()把超出上下文范围的老消息压成摘要塞进system，原文保留，攒够20条新老消息才重摘一次。
5. 气泡操作：已有(编辑/删除/重生/TTS)。
6. Play记忆共享：processPlayMemories里改成addMemoryForAi(curAiId,mem)同时写主助手记忆+角色记忆。
7. 网页读取失败显示HTTP/网络错误(不再静默)。
8. 状态栏：顶部标题旁加绿点表示在线。

另外：SP人设改成设置里直接编辑大框(spDirectPage绑定ai.sp)，摘掉了SP卡片叠加(getAiSpText只返回ai.sp)。新增"重新开始"按钮(resetChat只清聊天保留其它，两次确认)。侧边栏顶部大头像改成直接换以礼头像(ai.avatar)。

踩坑：删HTML元素后init里残留$('goAiMgr').onclick会因null报错导致整个设置页按钮失效——删元素必须同时删对应js绑定。